{
  "internet": {
    "description": "Normal internet usage: everything on DHCP.",
    "ethernet": {
      "mode": "dhcp"
    },
    "wifi": {
      "mode": "dhcp"
    }
  },
  "ssh-link": {
    "description": "Direct ethernet cable to the laptop for SSH. Wifi is left alone so this device keeps normal internet access.",
    "ethernet": {
      "mode": "static",
      "address": "10.10.10.2",
      "prefix": 24,
      "gateway": null,
      "dns": []
    }
  },
  "static-wifi": {
    "description": "Give this Pi a fixed IP on its regular wifi network (e.g. so SSH access doesn't depend on a DHCP reservation), instead of the DHCP wifi in 'internet'. Assumes the wifi interface is already associated with that network (joined once via raspi-config, nmcli, or similar) - no ssid/passphrase needed since this isn't joining a new network, just changing the addressing on one it's already on. Adjust address/gateway/dns to match your actual network. Ethernet is left alone.",
    "wifi": {
      "mode": "static",
      "address": "192.168.1.50",
      "prefix": 24,
      "gateway": "192.168.1.1",
      "dns": ["192.168.1.1"]
    }
  },
  "peer-host": {
    "description": "Broadcast this Pi's own wifi network (hostapd) so another device can join directly over the air, e.g. for SSH when no shared router is available. Ethernet is left alone. Run 'internet' again to stop hosting and resume normal wifi. 'interface' pins the USB adapter (longer range) instead of the built-in one - remove it, or change it to match actual hardware, if there's only one wifi adapter.",
    "wifi": {
      "mode": "ap",
      "interface": "wlan1",
      "ssid": "pi-peer-link",
      "passphrase": "changeme123",
      "address": "192.168.50.1",
      "prefix": 24,
      "channel": 6,
      "band": "2.4ghz"
    }
  }
}
